Teste novo repositório git API

Nome do repositório remoto: api

Para commitar novas alterações:
git add .
git commit -m "Mensagem de commit"
git push api master



Para dar pull nas novas alterações, basta:
git pull
